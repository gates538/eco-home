- id: 'eco_home_zero_sprechi_spegnimento_luci'
  alias: 'Eco Home - Zero Sprechi Spegnimento Luci all Uscita'
  description: 'Spegne automaticamente tutte le luci della casa quando tutti i membri escono di casa'
  triggers:
  - trigger: state
    entity_id:
    - person.stefano
    - person.laura
    - person.simone
    - person.monica
    to: 'not_home'
    id: persona_uscita
  conditions:
  - condition: state
    entity_id: input_boolean.eco_home_attivo
    state: 'on'
  - condition: template
    value_template: "{{ states('person.stefano') not in ['home', 'casa'] and states('person.laura') not in ['home', 'casa'] and states('person.simone') not in ['home', 'casa'] and states('person.monica') not in ['home', 'casa'] }}"
  actions:
  - action: light.turn_off
    target:
      entity_id:
      - light.luceambiente
      - light.faretti_cucina
    continue_on_error: true
  - action: input_text.set_value
    target:
      entity_id: input_text.eco_home_ultimo_messaggio
    data:
      value: "Zero Sprechi: Luci spente all'uscita di tutti"
  mode: single

- id: 'eco_home_luci_accese_pieno_giorno'
  alias: 'Eco Home - Zero Sprechi: Luci Accese in Pieno Giorno (> 1 Ora)'
  description: 'Suggerimento vocale discreto se la luce ambiente o i faretti restano accesi per oltre 60 minuti con pieno sole'
  triggers:
  - trigger: state
    entity_id:
    - light.luceambiente
    - light.faretti_cucina
    to: 'on'
    for:
      minutes: 60
  conditions:
  - condition: template
    value_template: "{{ not (now().hour >= 23 or now().hour < 7) }}"
  - condition: state
    entity_id: input_boolean.eco_home_attivo
    state: 'on'
  - condition: state
    entity_id: input_boolean.eco_home_silenzioso
    state: 'off'
  - condition: state
    entity_id: sun.sun
    state: 'above_horizon'
  - condition: template
    value_template: "{{ state_attr('sun.sun', 'elevation') | float(0) > 15 }}"
  - condition: template
    value_template: "{{ is_state('person.stefano', 'home') or is_state('person.laura', 'home') }}"
  - condition: template
    value_template: "{{ this.attributes.last_triggered is none or (now() - this.attributes.last_triggered).total_seconds() > 7200 }}"
  actions:
  - action: tts.speak
    target:
      entity_id: tts.google_translate_it_lt
    data:
      media_player_entity_id: media_player.nest_hub_sala
      language: "it"
      options:
        tld: "it"
      message: >-
        {{ [
          "Promemoria risparmio: le luci sono accese da un'ora con pieno sole fuori! Volete spegnerle?",
          "Avviso zero sprechi: c'è una bella luce naturale all'esterno, verificate se le luci interne servono ancora!",
          "Consiglio eco: splendida giornata di sole fuori, possiamo spegnere le luci e risparmiare corrente!"
        ] | random }}
  - action: input_text.set_value
    target:
      entity_id: input_text.eco_home_ultimo_messaggio
    data:
      value: "Zero Sprechi: Luci accese oltre 1h di giorno"
  mode: single
